import styled from 'styled-components/native'
import FlashMessage from 'react-native-flash-message'

export const NotifyFlashMessageContainer = styled(FlashMessage)`
  z-index: 9;
  padding: 15px;
  align-items: center;
  height: 50px;
`
